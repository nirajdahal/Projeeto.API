<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Projeeto Account Verification</title>
  <style>
    /* Global styles */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background-color: #0a1930;
      font-family: Arial, Helvetica, sans-serif;
    }

    /* Header styles */
    .header {
      display: flex;
      justify-content: center;
    }

    .logo {
      padding: 5px;
      background-color: #1f93ff;
      margin-right: 10px;
    }

    .logo h2 {
      color: #fff;
      font-size: 2rem;
      font-weight: bold;
      text-transform: uppercase;
    }

    /* Content styles */
    .content {
      background-color: #eee;
      border-radius: 3px;
      margin: 2rem auto;
      max-width: 600px;
      padding: 30px;
      text-align: center;
    }

    .content h2 {
      font-size: 3rem;
      margin-bottom: 1rem;
      text-transform: capitalize;
    }

    .content p {
      font-size: 1.6rem;
      line-height: 1.5;
      margin-bottom: 1rem;
    }

    /* Button styles */
    .btn {
      background-color: #007bff;
      border: none;
      border-radius: 3px;
      color: #fff;
      cursor: pointer;
      display: inline-block;
      font-size: 1.6rem;
      font-weight: bold;
      margin-top: 1rem;
      padding: 10px 20px;
      text-decoration: none;
      text-transform: uppercase;
    }

    .btn:focus,
    .btn:hover {
      background-color: #0062cc;
      outline: none;
    }

    .btn--success {
      background-color: #28a745;
    }

    .btn--danger {
      background-color: #dc3545;
    }

    /* Footer styles */
    .footer {
      color: #fff;
      font-size: 1.4rem;
      margin-top: 2rem;
      text-align: center;
    }

    .footer p {
      margin-bottom: 1rem;
    }

    .footer p:last-child {
      font-size: 1.6rem;
      font-weight: bold;
    }

    .footer a {
      color: #fff;
      font-weight: bold;
      text-decoration: none;
    }
  </style>
</head>

<body>
  <header class="header">
    <div class="logo">
      <h2>Projeeto</h2>
    </div>
  </header>
  <div class="content">
    <h2>Hello <span class="color-danger">{{name}}</span></h2>
    <p>Please use the link below to verify your account. This link is valid for 1 hour.</p>
    <a href="{{link}}" class="btn btn--success">Verify Account</a>
  </div>
  <footer class="footer">
    <p>Regards...</p>
    <p>Projeeto Team</p>
  </footer>
</body>

</html>